const FilterButtons = () => {
  return (
    <div className="flex gap-2">
      <button>전체</button>
      <button>진행중</button>
      <button>완료</button>
    </div>
  );
};

export default FilterButtons;
